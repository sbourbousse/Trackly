name: Deploy Vercel (Manuel)

on:
  workflow_dispatch:
    inputs:
      environment:
        description: 'Environnement de déploiement'
        required: true
        default: 'preview'
        type: choice
        options:
          - preview
          - production
      branch:
        description: 'Branche à déployer (pour preview)'
        required: false
        default: 'develop'
        type: string

jobs:
  deploy-vercel:
    name: Deploy to Vercel
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.branch }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies (root)
        run: npm install

      # Deploy frontend-business
      - name: Deploy frontend-business
        working-directory: ./frontend-business
        run: |
          npm install
          if [ "${{ github.event.inputs.environment }}" == "production" ]; then
            npx vercel deploy --prod --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd .
          else
            npx vercel deploy --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd . 2>&1 | tee deploy.log
            echo "URL: $(grep -o 'https://[^[:space:]]*\.vercel\.app' deploy.log | tail -1)"
          fi
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_BUSINESS }}

      # Deploy frontend-tracking
      - name: Deploy frontend-tracking
        working-directory: ./frontend-tracking
        run: |
          npm install
          if [ "${{ github.event.inputs.environment }}" == "production" ]; then
            npx vercel deploy --prod --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd .
          else
            npx vercel deploy --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd . 2>&1 | tee deploy.log
            echo "URL: $(grep -o 'https://[^[:space:]]*\.vercel\.app' deploy.log | tail -1)"
          fi
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_TRACKING }}

      # Deploy frontend-landing-page (si configuré)
      - name: Deploy frontend-landing-page
        if: ${{ secrets.VERCEL_PROJECT_ID_LANDING != '' }}
        working-directory: ./frontend-landing-page
        run: |
          npm install
          if [ "${{ github.event.inputs.environment }}" == "production" ]; then
            npx vercel deploy --prod --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd .
          else
            npx vercel deploy --yes --token ${{ secrets.VERCEL_TOKEN }} --cwd . 2>&1 | tee deploy.log
            echo "URL: $(grep -o 'https://[^[:space:]]*\.vercel\.app' deploy.log | tail -1)"
          fi
        env:
          VERCEL_ORG_ID: ${{ secrets.VERCEL_ORG_ID }}
          VERCEL_PROJECT_ID: ${{ secrets.VERCEL_PROJECT_ID_LANDING }}

      - name: Skip landing page (not configured)
        if: ${{ secrets.VERCEL_PROJECT_ID_LANDING == '' }}
        run: echo "⚠️ VERCEL_PROJECT_ID_LANDING not set, skipping landing page deployment"

      - name: Notify Deployment
        run: |
          echo "✅ Déploiement Vercel terminé"
          echo "Environnement: ${{ github.event.inputs.environment }}"
          echo "Branche: ${{ github.event.inputs.branch }}"
